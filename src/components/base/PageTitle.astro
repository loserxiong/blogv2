---
interface Props {
  title: string
  introduce: string
}
const { title, introduce } = Astro.props
---

<h1 class="text-[2rem] sm:text-[2.5rem] leading-tight tracking-tight">{title}</h1>
<p class="mt-3 text-base leading-relaxed text-muted-foreground">{introduce}</p>
